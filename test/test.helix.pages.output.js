const assert = require('assert');

describe('Testing the basic Helix Pages output', () => {
    it('is true', () => {
      assert.ok(true);
    });
  
    it('is false', () => {
        assert.ok(true);
    });
    
  });